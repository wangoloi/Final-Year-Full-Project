# Environment variables for the FastAPI app are documented here:
#   backend/.env.example
#
# Copy that file to either:
#   - backend/.env   (recommended for API-only work), or
#   - .env           (repo root; still loaded first, then backend/.env overrides)
