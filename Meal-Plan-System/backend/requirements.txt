# FastAPI backend
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
httpx>=0.25.0
pytest>=7.0.0

# Database
sqlalchemy>=2.0.0

# Auth
bcrypt>=4.0.0
PyJWT>=2.8.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# ML/RAG (optional - for semantic search)
scikit-learn>=1.3.0
# 2.2.x breaks with huggingface_hub>=0.20 (cached_download removed); keep aligned with chromadb stack
sentence-transformers>=3.0.0,<4
# 1.5+ has prebuilt wheels on Python 3.14; 0.4.x pulls chroma-hnswlib (often needs MSVC build on Windows)
chromadb>=1.5.0,<2
rapidfuzz>=3.0.0
typesense>=0.21.0
