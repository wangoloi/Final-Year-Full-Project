# FastAPI backend
fastapi>=0.104.0
uvicorn>=0.24.0
gunicorn>=21.0.0
python-multipart>=0.0.6
httpx>=0.25.0
pytest>=7.0.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# Auth
bcrypt>=4.0.0
PyJWT>=2.8.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
email-validator>=2.0.0

# Data processing (pin NumPy <2 for ChromaDB compatibility)
pandas>=2.0.0
numpy>=1.26.0,<2.0

# ML/RAG (optional - for semantic search)
# 2.2.x: huggingface_hub>=0.23 removed cached_download.
# 3.x: pulls newer transformers; can trigger TF/Keras import issues — stay on 2.x for API servers.
scikit-learn>=1.3.0
sentence-transformers>=2.7.0,<3.0.0
transformers>=4.34.0,<4.41.0
huggingface_hub>=0.20.0,<0.26.0
chromadb>=0.5.0,<0.7
# Chroma telemetry calls posthog.capture(distinct_id, event, props); posthog 3+ changed the API — pin for safety.
posthog>=2.4.0,<3.0.0
rapidfuzz>=3.0.0
typesense>=0.21.0
